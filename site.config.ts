/**
 * Site Configuration
 * 
 * Change this value when deploying to a different domain or repository.
 * 
 * Examples:
 * - GitHub Pages subdirectory: '/'
 * - GitHub Pages root (username.github.io): '/'
 * - Custom domain: '/'
 */
export const siteConfig = {
  // The base path for the site (must start and end with '/')
  basePath: '/',
  
  // Site metadata
  siteName: 'Metalværk',
  siteDescription: 'Handcrafted in Jewelry',
};
