/**
 * Site Configuration
 * 
 * Change this value when deploying to a different domain or repository.
 * 
 * Examples:
 * - GitHub Pages subdirectory: '/metalvaerk-showcase/'
 * - GitHub Pages root (username.github.io): '/'
 * - Custom domain: '/'
 */
export const siteConfig = {
  // The base path for the site (must start and end with '/')
  basePath: '/metalvaerk-showcase/',
  
  // Site metadata
  siteName: 'Metalværk',
  siteDescription: 'Exquisite collection of premium metal artworks and sculptures',
};
